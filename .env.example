# Copier ce fichier en `.env` et renseigner de vraies valeurs avant de lancer
# `docker compose up`. `.env` n'est jamais commité (voir .gitignore).

# Secret de signature des sessions (JWT), 32+ caractères aléatoires.
# Générer avec : openssl rand -hex 32
SESSION_SECRET=
